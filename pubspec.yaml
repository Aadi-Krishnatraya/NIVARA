name: nivara_app
description: "NIVARA — Preventive Welfare & Workload Engine for Uniformed Personnel (PRD 26186)."
publish_to: 'none'

version: 2.0.0+2

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  sqflite_sqlcipher: ^3.1.0
  path: ^1.9.0
  fl_chart: ^0.68.0
  tflite_flutter: ^0.12.1
  crypto: ^3.0.3
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8
  # x402 / Algorand — on-device x402 client (Wellness Exchange)
  http: ^1.2.2
  pointycastle: ^3.9.1
  convert: ^3.1.2
  camera: ^0.12.1
  google_mlkit_face_detection: ^0.15.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/branding/
